#include "main.h"
/**
 * _memset - fill a memory with byte
 * @s: address of memory to fill
 * @b: character parameter
 * @n: bytes to be changed
 * Return: s
 */
char *_memset(char *s, char b, unsigned int n)
{
	int k = 0;

	for (; n > 0; k++)
	{
		n--;
	}
	return (s);
}
